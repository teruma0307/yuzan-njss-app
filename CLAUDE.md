# Claude Code 作業指示

## プロジェクト目的
NJSS（入札情報速報サービス）からエクスポートした入札案件CSVを取り込み、
株式会社雄山（林様の父親の会社、防衛省発注の建設工事に入札資格を持つ）の
参加資格に合う入札案件を抽出し、入札スケジュールを一覧管理し、将来的には
毎朝の自動メール送信・見積書類作成補助を行うシステムを段階的に構築する。

（株式会社ポンプアップは入札に一切関与していないため、このプロジェクトの対象外。
ポンプアップ向けのeBay転売システムは同じ「AIの作業場」内の別プロジェクト
`ai_reselling_starter`であり、本プロジェクトとは無関係。）

## 現在の完成範囲
- NJSS案件CSVの読込・必須列バリデーション・列名正規化（2026-07-30に実際のNJSSエクスポート
  CSV3件で列構成を検証済み。想定していた列名とはかなり異なっていたため実データに合わせて修正した）
- 発注機関キーワード（防衛省）＋（雄山の資格業種13種 or 全省庁統一資格を要求する役務案件）＋
  資格有効期限による候補案件抽出（条件は`config/filter_settings.json`で管理）
- 業種マッチングはカテゴリ・トークン単位の完全一致（`src/bid_filter.py`の`_industry_matches`）。
  単純な部分一致だと「管」が「指定管理」に誤一致する等の実データで確認した誤検出があるため、
  この方式を採用している
- 資料等提出日（無ければ入札日）を基準にした締切ハイライト表示
- Streamlit画面でのアップロード→フィルタ→一覧表示→チェックボックスによる人間承認
- 承認済み案件のCSVダウンロード
- pytestによるテスト一式
- PyInstallerによるexe化（`run_standalone.py`が起動用エントリポイント）。Python環境の無い
  他のパソコンでも `雄山NJSS案件抽出_配布用`（exe＋configフォルダ）をコピーするだけで動く。
  ビルドコマンドはREADME.md参照。各パソコンに`.venv`をコピーするとPythonバージョン不一致で
  壊れることが実機で判明したため、この配布方法を採用した

## 雄山の入札資格について（事実、推測ではない）
2026-07-30にユーザーから提供された「資格審査結果通知書」（防衛省整備計画局建設制度官発行、
令和7年3月24日付、有効期限令和9年3月31日）に基づく。対象は防衛省発注工事のみ。
資格のある工事種別: 土木一式、大工、とび・土工・コンクリート、石、管、タイル・れんが・ブロック、
鋼構造物、鉄筋、舗装、しゅんせつ、機械器具設置、水道施設、解体（塗装を含むそれ以外は資格なし、
ユーザー本人が塗装の資格なしと確認済み）。他の発注機関（都道府県・市町村等）向け資格は今回対象外
（ユーザー確認済み、将来追加の可能性あり）。等級（土木一式D、その他大半C等級）はCSVの「入札資格」
列に情報があるが、1案件に複数の資格系統が混在する自由記述のため自動判定しない。

雄山は全省庁統一資格（物品の製造・販売・役務の提供等＝等級D、物品の買受け＝等級B、令和7年4月1日〜
令和10年3月31日、全国有効）も保有しており、ユーザーの指示で建設工事以外の「役務」案件（撤去・
据付作業、看板撤去等、入札資格に「全省庁統一資格」を含むもの）も候補に含めている。

## 実際のNJSS CSV列構成について（2026-07-30に3ファイルで確認した事実）
`検索条件名, 案件ID, 入札形式, 案件名, 案件概要URL, 機関, 機関所在地, 履行/納品場所, 案件公示日,
入札日, 説明会日, 資料等提出日, 入札資格, 業種, 案件概要, 案件備考, 落札結果公示日, 契約,
落札日(or契約締結日), 落札会社名, 落札会社住所, 落札理由, 落札価格, 落札単価, 落札評点,
落札結果備考, 予定価格, 予定単価, 入札結果詳細, 不調`。「開札日」「申込締切日」に相当する列は
存在しない（入札日が開札日を兼ね、資料等提出日が実質的な締切）。「業種」は改行区切りの複数値で
「舗装工事」のように「工事」が付く表記。エンコーディングはcp932（Shift-JIS）。

## 最重要ルール
- CSVに存在しない金額・資格要件・仕様をAIや推測で補完・でっちあげない。
- 案件名・発注機関名・金額など原文CSVの値をコードやAIで勝手に書き換えない。
- 動作未確認・裏取りしていない情報を確定情報として画面に表示しない。
- 案件への応札判断・書類提出前に必ず人間の最終承認を残す。
- 等級（ランク）要件はCSVだけで自動判定せず、必ず案件詳細を人が確認する（画面上に注意書きを表示する）。
- 雄山の入札資格の有効期限が切れている場合、他の条件を満たしていても候補として表示しない。
- APIキーや秘密情報をコードへ直書きしない。`.env`はGitへコミットしない。
- 実際のNJSS CSVサンプルと列名・仕様が異なっていた場合、AIが独断で仕様を推測して
  合わせにいかず、必ずユーザーに確認してから対応する。
- NJSSサイトへの自動ログイン・自動スクレイピングは行わない（NJSS利用規約で自動収集が禁止され、
  違反時の損害賠償責任が明記されているため）。CSVエクスポートは人が毎朝手動で行う前提を崩さない。
- ポンプアップは入札に一切関与しない。ポンプアップ向けの機能をこのプロジェクトに追加しない。
- 「いくらで入札すれば落札できるか」を予測・提案する機能を実装しない。受付中の案件には
  予定価格・落札価格の情報が無く、断定的な金額提示は事実に基づかない推測になるため。
  過去の落札結果CSVがあれば、類似案件の実際の落札価格を事実として一覧表示することはできるが、
  それも「予測」ではなく「過去の公開事実の集計」として提示する。
- 変更後は`pytest`を実行する。

## 次に実装する順番
1. 毎朝の自動メール送信（Outlook/M365のSMTP経由。送信元アカウントの管理者はユーザー自身のため
   SMTP AUTH設定は自分で対応可能と確認済み）と、Windowsタスクスケジューラでの毎朝自動実行登録
   （人が毎朝NJSSでCSVをエクスポートして所定フォルダに置く→スクリプトがそれを検知して処理・送信）
2. 落札結果CSV（開札・契約済み案件のエクスポート）を使った、類似案件の過去落札価格の一覧化
   （金額の予測ではなく、公開されている事実の集計として実装する）
3. SQLiteによる案件データの蓄積保存
4. 案件ID（またはURL）をキーとした重複登録防止
5. 蓄積した過去案件の検索・絞り込みUI
6. 案件情報から見積書・申請書類のたたき台をAIで生成する機能
   （金額・資格要件はCSVに存在する値のみ使用し、AIによる新規の数値・要件の創作を
   禁止するガードレール付きで実装する）
7. 雄山の他発注機関（都道府県・市町村等）向け資格取得時の設定追加

## Claude Codeへの最初の依頼
このリポジトリ全体を確認し、まずテストを実行してください。
その後、既存機能を壊さずに毎朝の自動メール送信機能を追加してください。
所定フォルダに置かれたCSVを検知し、雄山の抽出条件で候補を絞り込んで、
Outlook/M365のSMTP経由で担当者宛にメール送信してください。
実装前に変更計画を示し、実装後にテスト結果と変更ファイルを報告してください。
